<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HackatonMilu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Set the background color -->
        <item name="android:windowBackground">@color/background_color</item>


        <!-- Set the button color -->
        <item name="colorButtonNormal">@color/button_color</item>

        <!-- Set the text color -->
        <item name="android:textColor">@color/text_color</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/toolBar_color</item>

        <item name="android:navigationBarColor" tools:targetApi="l">@color/toolBar_color</item>
    </style>


    <style name="Theme.HackatonMilu" parent="Base.Theme.HackatonMilu" />
</resources>